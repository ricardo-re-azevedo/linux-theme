width = 30%

corner-radius = ${borderRadius}
outline-width = 0
border-width = ${borderWidth}

font = ${font}
font-size = 12

text-color       = ${textColor}
prompt-color     = ${textColorHeading}
selection-color  = ${textColorSelected}
background-color = ${background}
border-color     = ${windowBorder}